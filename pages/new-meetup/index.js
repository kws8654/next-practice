import NewMeetupForm from '../../components/meetups/NewMeetupForm';

const NewMeetupPage = () => {
  const addMeetup = (enteredData) => {};

  return <NewMeetupForm addMeetup={addMeetup} />;
};

export default NewMeetupPage;
